apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: strategy-runner
  namespace: quant-system
spec:
  namespaceSelector:
    matchNames:
      - quant-system
  selector:
    matchLabels:
      app: strategy-runner
  endpoints:
    - port: http
      path: /metrics
      interval: 5s
      scrapeTimeout: 3s
