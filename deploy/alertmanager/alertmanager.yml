global:
  resolve_timeout: 5m

route:
  receiver: feishu
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: feishu
    webhook_configs:
      - url: 'http://admin-api:8090/api/v1/alerts/webhook'
        send_resolved: true
